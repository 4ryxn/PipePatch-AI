# Phase 1 has no required environment variables.
# Reserved for a future backend-only OpenAI integration. Keep this value secret
# and never copy it into mobile configuration or source code.
OPENAI_API_KEY=
