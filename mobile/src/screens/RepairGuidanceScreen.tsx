import { StyleSheet, Text, View } from "react-native";
import { AppButton } from "../components/AppButton";
import { colors } from "../theme";
import type { RepairGuidanceResponse } from "../types/repairGuidance";
export function RepairGuidanceScreen({ guidance, onBack }: { guidance: RepairGuidanceResponse; onBack: () => void }): React.JSX.Element { const eligible = guidance.decision === "eligible"; return <><Text style={s.eyebrow}>DETERMINISTIC REPAIR GUIDANCE</Text><Text style={s.title}>{eligible ? "Safe clean-cut repair plan" : "Guidance is blocked"}</Text>{!eligible && <List title="What to do next" items={guidance.reasons} />}{eligible && <><List title="Before you begin" items={guidance.preparation_checklist} /><List title="Materials and tools" items={guidance.materials_tools_checklist} /><List title="High-level repair steps" items={guidance.steps.map((item, index) => `${index + 1}. ${item}`)} /><List title="Stop and call a professional if…" items={guidance.stop_conditions} /><List title="After the repair" items={guidance.post_repair_verification} /></>}<List title="Important limitations" items={guidance.limitations} /><AppButton label="Back to assessment" variant="secondary" onPress={onBack} /></>; }
function List({ title, items }: { title: string; items: string[] }): React.JSX.Element { return <View style={s.card}><Text style={s.cardTitle}>{title}</Text>{items.map((item) => <Text key={item} style={s.body}>• {item}</Text>)}</View>; }
const s = StyleSheet.create({ eyebrow: { color: colors.limeDark, fontWeight: "800" }, title: { color: colors.green, fontSize: 30, fontWeight: "800" }, card: { backgroundColor: colors.white, borderColor: colors.creamDark, borderRadius: 16, borderWidth: 1, gap: 8, padding: 16 }, cardTitle: { color: colors.green, fontSize: 18, fontWeight: "800" }, body: { color: colors.ink, fontSize: 16, lineHeight: 23 } });
