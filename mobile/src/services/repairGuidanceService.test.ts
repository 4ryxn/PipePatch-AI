/* eslint-disable import/first */
const fetchMock = jest.fn();
jest.mock("../config/api", () => ({ apiBaseUrl: "http://example.test" }));
import { requestRepairGuidance } from "./repairGuidanceService";
const response = { decision: "eligible", repair_method_id: "two_slip_coupling_section_replacement", reasons: [], preparation_checklist: [], materials_tools_checklist: [], steps: [], stop_conditions: [], post_repair_verification: [], limitations: [], source_links: [] };
const payload = { analysis: { is_mock: false, supported_case: true, material: null, pipe_schedule: null, nominal_size: null, damage_type: null, confidence: .8, summary: "", evidence: [], unknowns: [], safety_flags: [], next_action: "" }, confirmations: { line_type: "outdoor_irrigation" as const, outdoor_irrigation: "yes" as const, water_supply_shut_off: "yes" as const, pvc_schedule_40_marking: "yes" as const, nominal_size: "1/2" as const, clean_transverse_cut: "yes" as const, no_additional_damage: "yes" as const, straight_section: "yes" as const, safely_away_from_components: "yes" as const, pipe_ends_accessible: "yes" as const }, measurement: { status: "measured" as const, estimated_outer_diameter_mm: 21.3, estimated_gap_mm: 50, quality_score: .9, pixels_per_mm: 5, marker_id: 23 as const, known_marker_side_mm: 50 as const, suggested_nominal_size: "1/2" as const, gap_range_status: "within_mvp_range" as const, limitations: [], retake_reasons: [] } };
beforeEach(() => { Object.defineProperty(global, "fetch", { configurable: true, value: fetchMock }); fetchMock.mockResolvedValue({ ok: true, json: async () => response }); });
test("posts complete ephemeral guidance context", async () => { await expect(requestRepairGuidance(payload, new AbortController().signal)).resolves.toEqual(response); expect(fetchMock).toHaveBeenCalledWith("http://example.test/api/v1/repair-guidance", expect.objectContaining({ method: "POST", headers: { "Content-Type": "application/json" } })); });
