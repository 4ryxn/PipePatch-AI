import { canStartCapture, cancellationResult, isCurrentOperation, permissionMessage, permissionState, shouldOfferSettings } from "./captureController";
test.each([["camera", { granted: true, canAskAgain: true }, "idle"], ["camera", { granted: false, canAskAgain: true }, "denied_camera"], ["camera", { granted: false, canAskAgain: false }, "blocked_camera"], ["library", { granted: true, canAskAgain: true }, "idle"], ["library", { granted: false, canAskAgain: true }, "denied_library"], ["library", { granted: false, canAskAgain: false }, "blocked_library"]] as const)("maps mocked %s permission results", (resource, result, expected) => expect(permissionState(resource, result)).toBe(expected));
test("uses resource-specific permission messages and Settings guidance", () => { expect(permissionMessage("denied_camera")).toContain("Camera"); expect(permissionMessage("denied_library")).toContain("Photo library"); expect(shouldOfferSettings("blocked_camera")).toBe(true); expect(shouldOfferSettings("blocked_library")).toBe(true); expect(shouldOfferSettings("unavailable")).toBe(true); expect(shouldOfferSettings("denied_camera")).toBe(false); });
test("cancellation is a safe no-op without an image or flow advance", () => expect(cancellationResult()).toEqual({ imageCreated: false, advance: false, error: null }));
test("guards stale results and duplicate actions", () => { expect(isCurrentOperation(4, 4)).toBe(true); expect(isCurrentOperation(4, 5)).toBe(false); expect(canStartCapture(false)).toBe(true); expect(canStartCapture(true)).toBe(false); });
