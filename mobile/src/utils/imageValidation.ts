import { imageConfig } from "../config/image";
import type { ImageCandidate } from "../types/image";
export type ValidationResult = { valid: true } | { valid: false; message: string };
const extensionMimes: Record<string, string> = { jpg: "image/jpeg", jpeg: "image/jpeg", png: "image/png", webp: "image/webp" };
const supported = new Set(Object.values(extensionMimes));
export function extensionFromUri(uri: string): string | null { const segment = uri.split("?")[0].slice(uri.split("?")[0].lastIndexOf("/") + 1); const dot = segment.lastIndexOf("."); return dot > 0 && dot < segment.length - 1 ? segment.slice(dot + 1).toLowerCase() : null; }
export function validateImage(candidate: ImageCandidate): ValidationResult { if (!candidate.localUri || !Number.isInteger(candidate.width) || !Number.isInteger(candidate.height) || candidate.width <= 0 || candidate.height <= 0) return { valid: false, message: "This photo has invalid dimensions. Choose or capture another image." }; const extension = extensionFromUri(candidate.localUri); const extensionMime = extension ? extensionMimes[extension] : null; const mime = candidate.mimeType?.toLowerCase() ?? null; if (mime && !supported.has(mime)) return { valid: false, message: "Use a JPG, PNG, or WebP photo." }; if (extension && !extensionMime) return { valid: false, message: "Use a JPG, PNG, or WebP photo." }; if (!mime && !extensionMime) return { valid: false, message: "This photo has no readable format information. Choose another image." }; if (mime && extensionMime && mime !== extensionMime) return { valid: false, message: "The photo format information is inconsistent. Choose another image." }; if (candidate.width < imageConfig.minDimension || candidate.height < imageConfig.minDimension) return { valid: false, message: `Use a photo at least ${imageConfig.minDimension} × ${imageConfig.minDimension} pixels.` }; return { valid: true }; }
