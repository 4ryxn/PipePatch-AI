import { initialPartsRequestState, parseQuote, partsBlock, partsRequestReducer } from "./partsPolicy";
const analysis={is_mock:false,supported_case:true,material:null,pipe_schedule:null,nominal_size:null,damage_type:null,confidence:.8,summary:"",evidence:[],unknowns:[],safety_flags:[],next_action:""}; const assessment={decision:"eligible" as const,reasons:[],safety_warnings:[],confirmed_pipe_size:"1/2" as const,repair_method_id:"two_slip_coupling_section_replacement" as const,parts:[],tools:[]}; const confirmations={line_type:"outdoor_irrigation" as const,outdoor_irrigation:"yes" as const,water_supply_shut_off:"yes" as const,pvc_schedule_40_marking:"yes" as const,nominal_size:"1/2" as const,clean_transverse_cut:"yes" as const,no_additional_damage:"yes" as const,straight_section:"yes" as const,safely_away_from_components:"yes" as const,pipe_ends_accessible:"yes" as const}; const measurement={status:"measured" as const,estimated_outer_diameter_mm:21.3,estimated_gap_mm:50,quality_score:.9,pixels_per_mm:5,marker_id:23 as const,known_marker_side_mm:50 as const,suggested_nominal_size:"1/2" as const,gap_range_status:"within_mvp_range" as const,limitations:[],retake_reasons:[]};
test("allows only eligible Parts navigation and blocks every unsafe context",()=>{expect(partsBlock(analysis,assessment,measurement,confirmations)).toBeNull();expect(partsBlock({...analysis,is_mock:true},assessment,measurement,confirmations)).toMatch(/Demo/);expect(partsBlock(analysis,{...assessment,decision:"needs_more_information"},measurement,confirmations)).toMatch(/assessment/);expect(partsBlock(analysis,assessment,null,confirmations)).toMatch(/measurement/);expect(partsBlock(analysis,assessment,{...measurement,status:"needs_retake"},confirmations)).toMatch(/measurement/);expect(partsBlock(analysis,assessment,{...measurement,gap_range_status:"above_mvp_range"},confirmations)).toMatch(/gap/);expect(partsBlock(analysis,assessment,{...measurement,suggested_nominal_size:null},confirmations)).toMatch(/ambiguous/);expect(partsBlock(analysis,assessment,{...measurement,suggested_nominal_size:"1"},confirmations)).toMatch(/conflicts/);});
test("parses empty valid and invalid quote values",()=>{expect(parseQuote("")).toBeNull();expect(parseQuote("200")).toBe(200);for(const value of ["-1","abc","Infinity","100001"])expect(parseQuote(value)).toBe("invalid");});
test("guards duplicate stale cancel and retry request lifecycle",()=>{const loading=partsRequestReducer(initialPartsRequestState,{type:"START",id:1});expect(partsRequestReducer(loading,{type:"START",id:2})).toBe(loading);expect(partsRequestReducer(loading,{type:"SUCCESS",id:2})).toBe(loading);expect(partsRequestReducer(loading,{type:"CANCEL",id:1}).status).toBe("idle");const failed=partsRequestReducer(loading,{type:"FAILURE",id:1});expect(partsRequestReducer(failed,{type:"RETRY",id:2})).toEqual({status:"loading",id:2});});
