import { describe,it,expect,vi } from "vitest"; import { analyze } from "./api"; describe("analysis request",()=>{it("uses multipart without manually setting a boundary",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>({is_mock:true,supported_case:false,confidence:0,summary:"",evidence:[],unknowns:[],safety_flags:[],next_action:""})}));await analyze(new File(["x"],"pipe.jpg",{type:"image/jpeg"}));expect(fetch).toHaveBeenCalledWith(expect.stringContaining("/api/v1/analyze"),expect.objectContaining({method:"POST",body:expect.any(FormData)}));});it("returns a blocked mock result unchanged",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>({is_mock:true,supported_case:false,damage_category:"unknown_or_unsupported",confidence:0,summary:"",evidence:[],unknowns:[],safety_flags:[],next_action:"Retake"})}));expect((await analyze(new File(["x"],"pipe.jpg"))).is_mock).toBe(true);});});
